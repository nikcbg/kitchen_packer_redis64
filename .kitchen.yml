---
driver:
  name: vagrant

provisioner:
  name: shell

platforms:
  - name: vbox/redis64_2
    driver:
      box: redis64_2
      provider: virtualbox
 
verifier:
  name: inspec

suites:
  - name: default
